<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework:Event/etc/events.xsd">
    
    <!-- Fires after product is saved in admin -->
    <event name="catalog_product_save_after">
        <observer name="training_product_save" instance="AlpineCommerce\Training\Observer\ProductSaveAfter"/>
    </event>

    <!-- Fires after order is placed -->
    <event name="sales_order_place_after">
        <observer name="training_order_placed" instance="AlpineCommerce\Training\Observer\OrderPlacedAfter"/>
    </event>

    <!-- Fires before checkout -->
    <event name="checkout_controller_before_action">
        <observer name="training_checkout_before" instance="AlpineCommerce\Training\Observer\CheckoutBefore"/>
    </event>

    <!-- Fires after customer login -->
    <event name="customer_customer_login">
        <observer name="training_customer_login" instance="AlpineCommerce\Training\Observer\CustomerLogin"/>
    </event>

</config>
